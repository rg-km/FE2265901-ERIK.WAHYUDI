// deklarasi set
const number_set = new Set([1, 2, 3, 8, 4, 2, 8]);
const animal_set = new Set(['dog', 'cat', 'elephant']);

// deklarasi array
const number_array = [1, 2, 3, 8, 4, 2, 8]
const animal_array = ['dog', 'cat', 'elephant']

// Jika kita print number_array dan animal_array, hasilnya akan sama dengan
// yang dideklarasikan, namun apa yang akan dihasilkan set?

console.log(number_array); // result: 1, 2, 3, 8, 4, 2, 8
console.log(animal_array); // result: 'dog', 'cat', 'elephant'